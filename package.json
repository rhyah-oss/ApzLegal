{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test:regression": "node scripts/phase1-regression.mjs",
    "test:unit": "pnpm --filter @workspace/scripts run test",
    "test:phase1": "pnpm --filter @workspace/api-spec run codegen && pnpm --filter @workspace/api-server run typecheck && pnpm --filter @workspace/apz-legal run typecheck && PORT=${PORT:-18593} BASE_PATH=${BASE_PATH:-/} pnpm --filter @workspace/apz-legal run build && pnpm test:regression && node scripts/phase1-browser-smoke.mjs"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  }
}
