scrape_configs:
  - job_name: apz-legal
    metrics_path: /api/metrics
    scrape_interval: 30s
    authorization:
      type: Bearer
      credentials_file: /run/secrets/apz_metrics_token
    static_configs:
      - targets: ['apz-legal-web:8080']
rule_files:
  - /etc/prometheus/apz-alerts.yml
